import { taskTypeFormatter } from 'bullet/helpers/task-type-formatter';
import { module, test } from 'qunit';

module('Unit | Helper | task type formatter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskTypeFormatter([42]);
  assert.ok(result);
});
