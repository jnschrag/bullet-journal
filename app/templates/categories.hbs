add shit

<form class="form-inline" {{action "addCategory" on="submit"}}>
	<div class="form-group">
		{{input value=name placeholder="type to add a new category..." class="form-control"}}
	</div>
	<button type="submit" class="btn btn-default btn-primary" disabled={{nameEmpty}}>Add</button>
</form>
